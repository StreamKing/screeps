/**
 * Defender role mainly used for Defending a room. 
 * Should work togheter with healers.
 */
var roleDefender = {

    /** @param {Creep} creep **/
    run: function(creep) {
    
    }
	 
};

module.exports = roleDefender;
